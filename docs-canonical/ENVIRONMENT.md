# Environment

<!-- docguard:version 0.1.0 -->
<!-- docguard:status draft -->
<!-- docguard:last-reviewed 2026-04-15 -->
<!-- docguard:generated true -->

> **Auto-generated by DocGuard.** Review and refine this document.

| Metadata | Value |
|----------|-------|
| **Status** | ![Status](https://img.shields.io/badge/status-draft-yellow) |
| **Version** | `0.1.0` |

---

## Prerequisites

| Tool | Version | Installation |
|------|---------|-------------|
| Node.js | 20.x 이상 | https://nodejs.org |
| pnpm | 10.x | `npm install -g pnpm` |
| TypeScript | ^5.8.2 | `pnpm install` 시 자동 설치 |

## Environment Variables

이 프로젝트는 순수 프론트엔드 monorepo로, **런타임 환경 변수가 없다**. 모든 앱은 Mock 데이터를 사용하며 외부 API를 호출하지 않는다.

| Variable | Category | Required | Example | Description |
|----------|----------|:--------:|---------|-------------|
| `VITE_APP_TITLE` | UI | — | `AI Wiki Portal` | 앱 타이틀 (선택사항) |
| `STORYBOOK_PORT` | 개발 도구 | — | `6006` | Storybook 개발 서버 포트 |

> **참고**: 실제 서비스 연동 시(GitHub API, Confluence API) 추가 환경 변수가 필요하나 현재 Mock 범위에서는 불필요.

## Setup Steps

```bash
# 1. 저장소 클론
git clone <repository-url>
cd generate-with-spec-kit

# 2. 의존성 설치
pnpm install

# 3. 디자인 토큰 빌드 (앱이 의존)
pnpm --filter @myorg/tokens build

# 4. 개발 서버 실행
# UI 컴포넌트 라이브러리 (Storybook)
pnpm --filter @myorg/ui storybook

# AI Wiki Portal 앱
pnpm --filter @myorg/ai-wiki-portal dev

# SFood 앱
pnpm --filter @myorg/sfood-by-codex dev

# 5. 전체 빌드
pnpm build

# 6. 전체 테스트
pnpm test
```

## Package Commands

| 패키지 | 명령 | 설명 |
|-------|------|------|
| `@myorg/tokens` | `pnpm --filter @myorg/tokens build` | 토큰 빌드 |
| `@myorg/tokens` | `pnpm --filter @myorg/tokens test` | 토큰 테스트 |
| `@myorg/ui` | `pnpm --filter @myorg/ui storybook` | Storybook 개발 서버 |
| `@myorg/ui` | `pnpm --filter @myorg/ui build-storybook` | Storybook 정적 빌드 |
| `@myorg/ai-wiki-portal` | `pnpm --filter @myorg/ai-wiki-portal dev` | 앱 개발 서버 |
| `@myorg/ai-wiki-portal` | `pnpm --filter @myorg/ai-wiki-portal test` | 앱 테스트 |

---

## Revision History

| Version | Date | Author | Changes |
|---------|------|--------|---------|
| 0.1.0 | 2026-04-15 | DocGuard Generate | Auto-generated (0 env vars found) |

---

## Standards Reference

> **Aligned with**: 12-Factor App Methodology
>
> **Sections covered**: Environment Variables, Config Separation, Setup Steps, Provider Configuration
>
> **Reference**: Wiggins, A. "The Twelve-Factor App." https://12factor.net
>
> *Standards alignment inspired by RAG-grounded generation (Lopez et al., AITPG, IEEE TSE 2026).*
